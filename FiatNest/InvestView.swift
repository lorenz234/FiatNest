import SwiftUI

struct InvestView: View {
    var body: some View {
        VStack {
            Text("Invest")
                .font(.title)
            Spacer()
        }
        .padding()
    }
} 